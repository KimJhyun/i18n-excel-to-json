export interface Vitei18nExcelJsonOptions {
    /**
     * Excel file to parse i18n message JSON
     */
    inputPath: string;
    /**
     * Path for export JSON files
     *
     * Defaults to './'
     */
    outputPath?: string;
}
