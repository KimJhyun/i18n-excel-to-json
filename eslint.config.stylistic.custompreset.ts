import type { StylisticCustomizeOptions } from '@stylistic/eslint-plugin';

const preset: StylisticCustomizeOptions = {
    arrowParens: true,
    braceStyle: '1tbs',
    indent: 4,
    jsx: false,
    quoteProps: 'as-needed',
    semi: true,
};

export default preset;
